%!TEX program = lualatex
\documentclass{article}
\usepackage{luanodelist}

\begin{document}
\directlua{
  local glyph = node.new(node.id('glyph'))
  glyph.char = 34

  luanodelist.analyze(glyph)
}

lorem
\end{document}
