%!TEX program = lualatex
\documentclass{article}
\usepackage{tree}

\begin{document}
\directlua{
  local glyph = node.new(node.id('glyph'))
  glyph.char = 34

  tree.analyze(glyph)
}

lorem
\end{document}
