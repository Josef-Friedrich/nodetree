%!TEX program = lualatex
\documentclass{article}
\usepackage{nodetree}

\begin{document}
\directlua{
  local glyph = node.new(node.id('glyph'))
  glyph.char = 69

  nodetree.analyze(glyph)
}

lorem
\end{document}
